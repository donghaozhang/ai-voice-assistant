# Voice Agent Dependencies
openai>=1.0.0
crewai>=0.70.0
elevenlabs>=1.0.0
mem0ai>=0.1.0
pyaudio>=0.2.11
python-dotenv>=1.0.0
pygame>=2.0.0

# Built-in modules (no installation needed):
# wave, tempfile, os

# Additional dependencies for audio processing
# ffmpeg (install via conda: conda install -c conda-forge ffmpeg)